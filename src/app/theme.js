'use client'

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#bef264',
    },
    secondary: {
      main: '#134e4a',
    },
    tertiary: {
      main: '#08444F'
    }
  },
});

export default theme;